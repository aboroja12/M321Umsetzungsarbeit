server:
  port: 8082

spring:
  application:
    name: CART-SERVICE

  data:
    redis:
      host: localhost
      port: 6379

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
